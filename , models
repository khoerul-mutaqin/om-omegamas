[1mdiff --git a/cdp_wip_so_manufacture_reports.zip b/cdp_wip_so_manufacture_reports.zip[m
[1mindex b4d1154..8d30ce8 100644[m
Binary files a/cdp_wip_so_manufacture_reports.zip and b/cdp_wip_so_manufacture_reports.zip differ
[1mdiff --git a/cdp_wip_so_manufacture_reports/models/models.py b/cdp_wip_so_manufacture_reports/models/models.py[m
[1mindex 26c3df5..284af1e 100644[m
[1m--- a/cdp_wip_so_manufacture_reports/models/models.py[m
[1m+++ b/cdp_wip_so_manufacture_reports/models/models.py[m
[36m@@ -13,7 +13,9 @@[m [mclass Picking(models.Model):[m
         if so:[m
             for rec in self:[m
                 lot_ids = rec.mapped('move_ids_without_package.lot_ids')[m
[31m-                so.cdp_create_report_valuation(lot_ids=lot_ids)[m
[32m+[m[32m                product_id = rec.mapped('move_ids_without_package.product_id.id')[m
[32m+[m[32m                # raise UserError(f"{product_id} -  {lot_ids}")[m
[32m+[m[32m                so.cdp_create_report_valuation(lot_ids=lot_ids,product_id=product_id,origin=rec.name)[m
          [m
     def cdp_open_report_valuation(self):[m
         """[m
[36m@@ -24,7 +26,7 @@[m [mclass Picking(models.Model):[m
         if so:[m
             # buat atau update dulu report valuation[m
             self.cdp_create_report_valuation()[m
[31m-            line_id = self.mapped('sale_id.order_line.id')[m
[32m+[m[32m            line_id = self.mapped('name')[m
             model = 'cdp.report.valuation'[m
             domain = [('name', '=', line_id)]            [m
             return {[m
[36m@@ -77,7 +79,7 @@[m [mclass SaleOrder(models.Model):[m
 [m
 [m
 [m
[31m-    def cdp_create_report_valuation(self,lot_ids=None):[m
[32m+[m[32m    def cdp_create_report_valuation(self,lot_ids=None,product_id=None,origin=None):[m
         """[m
         Fungsi ini digunakan untuk membuat satu record valuation yang [m
         menampung seluruh Lot terkait produk yang dikonsumsi/dihasilkan.[m
[36m@@ -99,17 +101,19 @@[m [mclass SaleOrder(models.Model):[m
             real_mos = mrp_production_action.search(domain)[m
 [m
             # 2. Loop setiap baris Sale Order (FG)[m
[31m-            for line in record.order_line:[m
[32m+[m[32m            selected_so = record.order_line.filtered(lambda m: m.product_id.id in product_id)[m
[32m+[m[32m            # for line in record.order_line:[m
[32m+[m[32m            for line in selected_so:[m
                 # CEK APAKAH SUDAH ADA: Cari master berdasarkan kriteria unik[m
                 # Contoh: berdasarkan nama dan produk yang sama[m
                 master_valuation = cdp_report_valuation_action.search([[m
                     ('product_id', '=', line.product_id.id),[m
                     ('company_id', '=', record.company_id.id),[m
[31m-                    ('name', '=', line.id)[m
[32m+[m[32m                    ('name', '=', f"{origin}")[m
                 ], limit=1)[m
 [m
                 vals = {[m
[31m-                    'name': f"{line.id}",[m
[32m+[m[32m                    'name': f"{origin}",[m
                     'product_id': line.product_id.id,[m
                     'company_id': record.company_id.id,[m
                     'lot_ids': lot_ids,[m
[36m@@ -120,6 +124,7 @@[m [mclass SaleOrder(models.Model):[m
                     master_valuation.write(vals)[m
                     # Hapus baris lama agar tidak double saat di-create ulang di Step 4[m
                     master_valuation.line_ids.unlink() [m
[32m+[m[41m               [m
                 else:[m
                     # CREATE: Jika belum ada, buat baru[m
                     master_valuation = cdp_report_valuation_action.create(vals)[m
[36m@@ -131,20 +136,40 @@[m [mclass SaleOrder(models.Model):[m
                 [m
                 for mo in target_mos:[m
                     # Ambil Lot dari MO ini untuk digunakan di setiap line RM-nya[m
[31m-                    current_lot_id = mo.lot_producing_id.id[m
[32m+[m[32m                    # current_lot_id = mo.