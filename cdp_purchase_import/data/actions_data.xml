<odoo>
    <data noupdate="1">
        <!-- Server Action Template -->
        <record id="action_server_example" model="ir.actions.server">
            <field name="name">Run Purchase Action</field>
            <field name="model_id" ref="model_purchase_import"/>
            <field name="binding_model_id" ref="model_purchase_import"/>
            <field name="state">code</field>
            <field name="code">
#records = env['purchase.import'].search([('processed_purchase', '=', False)],order='id asc')
for record in records.sorted('order_number'):
    if not record.processed_purchase:
        record.action_process_purchase()
            </field>
        </record>
    </data>
</odoo>
