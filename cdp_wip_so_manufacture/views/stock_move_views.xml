<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <record id="cdp_wip_so_manufacture_inherit_wip_so_manufacture" model="ir.ui.view">
            <field name="name">cdp_wip_so_manufacture_inherit_wip_so_manufacture</field>
            <field name="model">stock.picking</field>
            <field name="inherit_id" ref="stock.view_picking_form"/>
            <field name="arch" type="xml">
                <xpath expr="//header" position="inside">
                    <!-- <button name="action_validate_and_make_mo_wip" string="Validate and Make WIP" type="object"
                        class="oe_highlight" invisible="state in ('draft', 'confirmed', 'done', 'cancel') and cdp_is_done != False"/> -->
                    <button name="action_make_mo_wip" string="Make MO WIP" type="object"
                        class="oe_highlight" invisible="state != 'done' or cdp_is_done == True"/>
                </xpath>
            </field>
        </record>

    </data>
</odoo>
