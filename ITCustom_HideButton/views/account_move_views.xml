<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <!-- Inherit account.move form view to hide Pay buttons based on user -->
        <record id="view_account_move_form_hide_button" model="ir.ui.view">
            <field name="name">account.move.form.hide.button</field>
            <field name="model">account.move</field>
            <field name="inherit_id" ref="account.view_move_form"/>
            <field name="arch" type="xml">
                <!-- Hide Register Payment button for invoices / receipts -->
                <xpath expr="//button[@name='action_register_payment' and @id='account_invoice_payment_btn']" position="attributes">
                    <attribute name="invisible">(state != 'posted' or payment_state not in ('not_paid', 'partial', 'in_payment') or move_type not in ('out_invoice', 'out_refund', 'in_invoice', 'in_refund', 'out_receipt', 'in_receipt') or invoice_has_outstanding) or hide_pay_button</attribute>
                </xpath>
                <!-- Hide Register Payment button for invoices / receipts (outstanding payments) -->
                <xpath expr="//button[@name='action_register_payment' and @id='account_invoice_payment_secondary_btn']" position="attributes">
                    <attribute name="invisible">(state != 'posted' or payment_state not in ('not_paid', 'partial', 'in_payment') or move_type not in ('out_invoice', 'out_refund', 'in_invoice', 'in_refund', 'out_receipt', 'in_receipt') or not invoice_has_outstanding) or hide_pay_button</attribute>
                </xpath>
            </field>
        </record>
    </data>
</odoo>
